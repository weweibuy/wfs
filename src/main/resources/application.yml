server:
  port: 8030

spring:
  application:
    name: wfs
  main:
    web-application-type: reactive
  cloud:
    gateway:
      routes:
      - id: brms-route
        uri: http://localhost:8050
        predicates:
        - Path=/wfs/rule/**
        filters:
        - StripPrefix=1